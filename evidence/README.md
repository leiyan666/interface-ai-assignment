# Evidence

Runtime JSONL logs, screenshots, and the generated artifact are kept here after the demo commands run.

`discovery_trajectory.json` contains the successful executed actions that the recorder/compiler consumed.
